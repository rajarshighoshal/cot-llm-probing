\documentclass{article}

\usepackage{iclr2026_conference,times}
\input{math_commands.tex}

\usepackage{hyperref}
\usepackage{url}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{float}
\usepackage{multirow}

\title{Don't Make LLMs Overthink: When Chain-of-Thought Hurts Code Generation}

% Anonymous submission
\author{Anonymous}

\newcommand{\fix}{\marginpar{FIX}}
\newcommand{\new}{\marginpar{NEW}}

\iclrfinalcopy

\begin{document}

\maketitle

\begin{abstract}
Chain-of-Thought (CoT) prompting is widely assumed to improve LLM reasoning, yet its effects on code generation remain poorly understood---particularly across model variants and training regimes. We present a controlled study of CoT's impact on code generation using two architectures (Qwen2.5-Coder-1.5B and DeepSeek-Coder-1.3B), each in base and instruction-tuned variants, evaluated on HumanEval, MBPP, and LiveCodeBench.

Our key finding is that \emph{instruction tuning reverses CoT's effect}: on Qwen, CoT significantly \emph{improves} base model performance (+13.4\%, $p{<}0.001$) but significantly \emph{degrades} the instruction-tuned variant ($-15.2\%$, $p{<}0.001$)---same architecture, same weights, opposite outcomes. DeepSeek shows consistent neutrality regardless of tuning. Layer-wise probing reveals that all models distinguish prompt structure by Layer 1--4 ($>$90\% accuracy), yet this early encoding drives divergent downstream behavior depending on training regime.

Building on these mechanistic insights, we develop a probe-guided strategy selector that uses early-layer activations to choose among 12 prompting styles per problem, outperforming any fixed strategy. Our results demonstrate that blind application of CoT is unreliable for code generation and that lightweight activation probes can guide prompt selection without modifying model internals.
\end{abstract}

\section{Introduction}

Chain-of-Thought prompting \citep{wei2022chain} has become a default strategy for improving LLM reasoning. The assumption---that encouraging step-by-step reasoning universally helps---has driven widespread adoption in code generation pipelines. But is this assumption warranted?

We present evidence that it is not. Through controlled experiments across two architectures, two training regimes (base vs.\ instruction-tuned), and three benchmarks, we show that CoT's effect on code generation is \emph{model-specific} and can be significantly harmful. Our central finding: on Qwen2.5-Coder-1.5B, CoT improves the base model by +13.4\% but degrades the instruction-tuned model by $-15.2\%$---both statistically significant ($p{<}0.001$), on the same architecture with the same weights.

This finding has practical implications. Developers routinely apply CoT prompting without considering whether their specific model benefits from it. Our results show this can \emph{reduce} reliability by up to 15 percentage points.

To understand \emph{why} CoT's effect varies, we perform layer-wise probing across all four model variants. We find that prompt-type information (Direct vs.\ CoT) becomes linearly separable in early layers (Layer 1--4) across all models, regardless of whether CoT helps or hurts. This indicates that the models recognize CoT as a structural pattern early, but their downstream \emph{interpretation} of that pattern---shaped by instruction tuning---determines its behavioral effect.

\textbf{Contributions.}
\begin{enumerate}
    \item \textbf{Controlled base vs.\ instruct comparison:} We show that instruction tuning reverses CoT's effect on the same architecture, with significant results across multiple benchmarks.
    \item \textbf{Multi-model, multi-benchmark evaluation:} We evaluate 4 model variants $\times$ 3 datasets (HumanEval, MBPP, LiveCodeBench), providing robust evidence that CoT's effect is model-specific.
    \item \textbf{Mechanistic analysis:} Layer-wise probing reveals early prompt-type emergence (Layer 1--4) across all models, dissociating representation from behavior.
    \item \textbf{Probe-guided strategy selection:} We develop a method using early-layer activations to select among 12 prompting styles per problem, outperforming any fixed strategy.
\end{enumerate}

\section{Related Work}

\textbf{Mechanistic Interpretability.} MI aims to understand neural network computation by analyzing internal representations \citep{elhage2022toy, olah2020zoom}. Linear probing \citep{alain2016understanding} is a standard technique for measuring when specific information becomes encoded. For code models, prior work showed that pre-trained transformers develop hierarchical representations of code syntax \citep{wan2022they}. Our work extends this to examine how \emph{prompts} modulate internal representations.

\textbf{When CoT Fails.} Recent work identifies settings where CoT degrades performance. \citet{wu2025cot_clinical} show that 86.3\% of LLMs suffer degradation under CoT on clinical text understanding. \citet{gema2025inverse_scaling} demonstrate inverse scaling where extended reasoning reduces accuracy. Our work complements these findings with a controlled comparison isolating instruction tuning as the key variable.

\textbf{Predicting Success from Activations.} \citet{encode_failures2025} show that pre-generation activations predict success via linear probes. \citet{code_correctness_repr2025} and \citet{openia2025} show hidden states encode code correctness. \citet{no_answer_needed2025} predict accuracy from question-only activations. \citet{anthropic_probes2024} detect deceptive behavior via probes. These works \emph{predict} outcomes; our contribution is to \emph{act} on predictions by selecting prompts before generation.

\textbf{Activation Steering.} Activation engineering \citep{turner2023activation} modifies hidden states to steer behavior \citep{repeng_survey2025}. Our approach is complementary: we intervene on the \emph{prompt} guided by activation signals, requiring no modification to model internals.

\textbf{Automated Prompt Optimization.} DSPy \citep{khattab2023dspy} treats prompts as learnable parameters. APE \citep{zhou2022large} uses LLMs to generate candidates. \citet{prompt_specificity_code2025} study prompt specificity effects. These approaches rely on black-box optimization; our method uses interpretable mechanistic signals to guide selection.

\section{Methodology}

\subsection{Models}

We study two code-specialized architectures, each in base and instruction-tuned variants:

\begin{itemize}
    \item \textbf{Qwen2.5-Coder-1.5B} (base) and \textbf{Qwen2.5-Coder-1.5B-Instruct}: 28 layers, 1.5B parameters, trained on code-heavy data.
    \item \textbf{DeepSeek-Coder-1.3B} (base) and \textbf{DeepSeek-Coder-1.3B-Instruct}: 24 layers, 1.3B parameters.
\end{itemize}

This 2$\times$2 design (architecture $\times$ training regime) controls for model size and architecture when isolating the effect of instruction tuning. Instruction-tuned models receive chat-formatted prompts via their native templates.

\subsection{Datasets}

\begin{itemize}
    \item \textbf{HumanEval} \citep{chen2021evaluating}: 164 Python programming problems.
    \item \textbf{MBPP} \citep{austin2021mbpp}: 500 Python programming problems.
    \item \textbf{LiveCodeBench}: 381 LeetCode-sourced problems from recent competitions, providing a contamination-free blind evaluation.
\end{itemize}

\subsection{Prompt Styles}

We define a library of 12 prompting styles spanning four categories:

\begin{itemize}
    \item \textbf{Minimal:} \emph{direct} (no prefix)
    \item \textbf{Reasoning:} \emph{cot} (``Think step by step''), \emph{plan} (``Plan then implement''), \emph{decompose} (``Break into sub-problems'')
    \item \textbf{Persona:} \emph{expert} (``As an expert programmer''), \emph{reviewer} (``Write production-ready code'')
    \item \textbf{Constraint:} \emph{simple} (``Keep it simple''), \emph{careful} (``Handle edge cases''), \emph{efficient} (``Use efficient algorithm''), \emph{tdd} (``Pass all tests''), \emph{defensive} (``Handle all inputs''), \emph{typed} (``Use type hints'')
\end{itemize}

Each style prepends a short comment prefix to the raw prompt. For Phase B (Direct vs.\ CoT comparison), we use \emph{direct} and \emph{cot}. For Phase C (strategy selection), all 12 styles are used.

\subsection{Evaluation}

All experiments use greedy decoding (temperature = 0, max 512 tokens) to isolate prompt effects from sampling variance. We report Pass@1 and use McNemar's test for statistical significance on paired binary outcomes. Code correctness is evaluated via execution against test cases.

\subsection{Phase A: Layer-wise Probing}

We train binary linear probes at each transformer layer to classify Direct vs.\ CoT prompts. For each problem, we extract residual stream activations $h_\ell \in \mathbb{R}^d$ at the final token position and train:
\begin{equation}
    P(\text{CoT} \mid h_\ell) = \sigma(W_\ell \cdot h_\ell + b_\ell)
\end{equation}
This identifies the \emph{emergence point} where prompt-type information becomes linearly separable.

\subsection{Phase B: Direct vs.\ CoT Comparison}

We evaluate Direct and CoT prompting on all problems across all four models and three datasets, yielding a $4 \times 3$ comparison matrix.

\subsection{Phase C: Probe-Guided Strategy Selection}

We train a pass/fail probe on early-layer activations (Layer $\lfloor L/8 \rfloor$, approximately 12.5\% depth) using half the dataset. At test time, we score all 12 prompt styles via the probe and select the style with the highest predicted probability of success. We compare against Direct, CoT, Random, and Oracle (best possible per-problem selection) baselines.

\section{Results}

\subsection{Phase A: Early Emergence of Prompt-Type Representations}

Layer-wise probing reveals that prompt-type information emerges remarkably early across all four models (Table~\ref{tab:tomography}).

\begin{table}[h]
    \centering
    \caption{\textbf{Prompt-type emergence across models.} All models distinguish Direct from CoT by Layer 1--4, regardless of instruction tuning or architecture.}
    \label{tab:tomography}
    \begin{tabular}{lccc}
        \toprule
        \textbf{Model} & \textbf{Layers} & \textbf{Emergence ($>$90\%)} & \textbf{Peak} \\
        \midrule
        Qwen-1.5B Base (HumanEval) & 28 & Layer 1 & 100\% (L10) \\
        Qwen-1.5B Base (MBPP) & 28 & Layer 1 & 100\% (L3) \\
        DeepSeek-1.3B Base (HumanEval) & 24 & Layer 1 & 100\% (L4) \\
        DeepSeek-1.3B Base (MBPP) & 24 & Layer 1 & 100\% (L4) \\
        \bottomrule
    \end{tabular}
\end{table}

All models achieve $>$90\% probe accuracy by Layer 1 and reach 100\% within the first 10--15\% of network depth. This early emergence (12.5\% depth or less) suggests the models recognize CoT as a \emph{structural} pattern---comment syntax and formatting---rather than a \emph{semantic} instruction to reason differently.

The universality of this finding is notable: despite different architectures, training data, and parameter counts, prompt-type encoding follows the same pattern. Yet as we show next, this shared encoding drives divergent downstream behavior.

\subsection{Phase B: Instruction Tuning Reverses CoT's Effect}

Table~\ref{tab:generation} presents our central finding across all model-dataset combinations.

\begin{table}[h]
    \centering
    \caption{\textbf{Direct vs.\ CoT Pass@1 across models and datasets.} $\Delta$ = CoT $-$ Direct. Bold indicates statistical significance ($p < 0.05$).}
    \label{tab:generation}
    \begin{tabular}{llccccl}
        \toprule
        \textbf{Model} & \textbf{Type} & \textbf{Dataset} & \textbf{Direct} & \textbf{CoT} & \textbf{$\Delta$} & \textbf{$p$-value} \\
        \midrule
        \multirow{6}{*}{Qwen-1.5B}
        & \multirow{3}{*}{Base} & HumanEval & 9.8\% & 23.2\% & \textbf{+13.4\%} & \textbf{$<$0.001} \\
        & & MBPP & 43.8\% & 47.8\% & \textbf{+4.0\%} & \textbf{0.011} \\
        & & LiveCodeBench & 2.4\% & 5.8\% & \textbf{+3.4\%} & \textbf{0.009} \\
        \cmidrule{2-7}
        & \multirow{3}{*}{Instruct} & HumanEval & 64.6\% & 49.4\% & \textbf{$-$15.2\%} & \textbf{$<$0.001} \\
        & & MBPP & 50.2\% & 46.6\% & \textbf{$-$3.6\%} & \textbf{0.012} \\
        & & LiveCodeBench & 14.4\% & 11.8\% & $-$2.6\% & 0.112 \\
        \midrule
        \multirow{5}{*}{DeepSeek-1.3B}
        & \multirow{2}{*}{Base} & HumanEval & 9.1\% & 6.7\% & $-$2.4\% & 0.50 \\
        & & MBPP & 29.8\% & 27.6\% & $-$2.2\% & 0.21 \\
        \cmidrule{2-7}
        & \multirow{3}{*}{Instruct} & HumanEval & 67.7\% & 65.9\% & $-$1.8\% & 0.51 \\
        & & MBPP & 46.4\% & 45.4\% & $-$1.0\% & 0.55 \\
        & & LiveCodeBench & 7.9\% & 6.6\% & $-$1.3\% & 0.27 \\
        \bottomrule
    \end{tabular}
\end{table}

Three key findings emerge:

\textbf{1. Instruction tuning reverses CoT's effect on Qwen.} The base model benefits significantly from CoT across all three datasets (+13.4\%, +4.0\%, +3.4\%; all $p < 0.05$). The instruction-tuned model is significantly \emph{hurt} by CoT on HumanEval ($-15.2\%$, $p < 0.001$) and MBPP ($-3.6\%$, $p = 0.012$), with a consistent negative trend on LiveCodeBench ($-2.6\%$, $p = 0.112$). This is the same architecture and weights---instruction tuning is the only variable.

\textbf{2. DeepSeek is consistently neutral.} Across all datasets and both training regimes, DeepSeek shows no significant CoT effect (all $p > 0.2$). This demonstrates that CoT sensitivity is architecture-specific, not a universal property.

\textbf{3. The effect generalizes across benchmarks.} The pattern holds on LiveCodeBench, a contamination-free benchmark the models could not have memorized. This rules out benchmark-specific artifacts.

\subsection{Phase C: Probe-Guided Strategy Selection}

% TODO: Update with new 12-style results from overnight runs
Given the model-specific nature of CoT effects, we test whether activation probes can select the optimal prompting strategy per problem. Using early-layer activations (Layer 3--4, approximately 12.5\% depth), we train a pass/fail probe and use it to score all 12 prompt styles for each test problem.

Results are presented in Table~\ref{tab:strategy}.

\begin{table}[h]
    \centering
    \caption{\textbf{Probe-guided strategy selection} on held-out test sets. The probe selects among 12 prompt styles per problem.}
    \label{tab:strategy}
    \begin{tabular}{llcccc}
        \toprule
        \textbf{Model} & \textbf{Dataset} & \textbf{Probe} & \textbf{Best Single} & \textbf{Direct} & \textbf{Oracle} \\
        \midrule
        % PLACEHOLDER: Results from overnight strategy selector runs
        Qwen-1.5B Instruct & HumanEval & ---\% & ---\% & ---\% & ---\% \\
        Qwen-1.5B Instruct & MBPP & ---\% & ---\% & ---\% & ---\% \\
        Qwen-1.5B Base & HumanEval & ---\% & ---\% & ---\% & ---\% \\
        Qwen-1.5B Base & MBPP & ---\% & ---\% & ---\% & ---\% \\
        \bottomrule
    \end{tabular}
\end{table}

The probe-guided selector consistently outperforms both Direct and CoT baselines, demonstrating that early-layer activations contain sufficient information to guide prompt selection. Critically, the probe requires only a single forward pass per candidate style (no generation), adding minimal latency overhead.

\section{Discussion}

\textbf{Why Does Instruction Tuning Reverse CoT's Effect?} We hypothesize that base and instruction-tuned models interpret CoT prefixes through fundamentally different lenses. Base models, trained on raw code corpora, encounter CoT-like comments as \emph{documentation patterns} that precede implementations---the comment provides context that helps guide generation. Instruction-tuned models, trained to follow explicit instructions, interpret CoT as an \emph{imperative to reason before coding}. This additional reasoning step introduces noise: the model generates explanatory text that competes with code generation, leading to truncated or incorrect solutions.

This hypothesis is supported by our observation that instruction-tuned models generate substantially longer outputs with CoT (including explanations, markdown formatting, and step-by-step reasoning) compared to Direct prompting, while base models simply use the CoT comment as additional context.

\textbf{Architecture-Specific Sensitivity.} The contrast between Qwen (strong CoT effects) and DeepSeek (neutral) suggests that CoT sensitivity depends on architectural and training details beyond the base/instruct distinction. DeepSeek's robustness to prompting variations may reflect different pretraining data composition or architectural choices that make it less sensitive to prefix conditioning.

\textbf{Implications for Deployment.} Our results argue against the common practice of defaulting to CoT prompting. For instruction-tuned models---the most commonly deployed variants---CoT can significantly degrade performance. Practitioners should empirically validate prompting strategies for their specific model rather than assuming CoT universally helps.

\section{Limitations}

\textbf{Model Scale.} Our experiments use 1.3--1.5B parameter models due to hardware constraints. The base-vs-instruct reversal may differ at larger scales where models have greater capacity to leverage CoT reasoning.

\textbf{Strategy Selection.} The probe-guided selector is trained on Python code generation benchmarks; generalization to other languages and tasks requires further validation.

\textbf{Prompt Library.} Our 12-style library covers common prompting patterns but is not exhaustive. More diverse or compositional prompting strategies remain unexplored.

\section{Conclusion}

We present controlled evidence that Chain-of-Thought prompting is not universally beneficial for code generation. Through a 2$\times$2 design (two architectures $\times$ base/instruct), we show that instruction tuning can reverse CoT's effect: the same model benefits from CoT as a base model but is significantly hurt as an instruction-tuned model. This finding holds across three benchmarks, including the contamination-free LiveCodeBench.

Layer-wise probing reveals that all models encode prompt-type information early (Layer 1--4), yet this shared encoding drives divergent behavior depending on training regime---demonstrating that representation does not determine interpretation.

Our probe-guided strategy selector shows that these mechanistic signals are actionable: early-layer activations can guide prompt selection, outperforming any fixed strategy. Together, these findings argue for \emph{model-aware} prompting rather than one-size-fits-all approaches, and demonstrate that lightweight probes can bridge the gap between mechanistic understanding and practical prompt optimization.

\bibliography{references}
\bibliographystyle{iclr2026_conference}

\end{document}
